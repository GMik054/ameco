/**
 * Webpack plugin to resolve LWC modules.
 */
export declare class LwcModuleResolverPlugin {
    fs: any;
    modules: any[];
    constructor(modules: any[]);
    apply(resolver: any): void;
    resolveModule(req: any, ctx: any, cb: any): any;
    isImplicitHTMLImport(importee: string, importer: string): boolean;
    resolveFile(req: any, ctx: any, cb: any): any;
}
